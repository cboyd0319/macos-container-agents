{
  "$schema": "docs/harness/schemas/feature-list.schema.json",
  "project": "RunHaven",
  "last_updated": "2026-06-26",
  "rules": {
    "single_active_feature": true,
    "passing_requires_evidence": true,
    "startup_context_must_stay_small": true
  },
  "status_legend": {
    "not_started": "Identified but no work or acceptance criteria committed yet.",
    "planned": "Scoped and queued; not the current active slice.",
    "active": "The single in-progress slice. Only one feature may be active at a time.",
    "blocked": "Cannot proceed until a recorded blocker clears.",
    "passing": "Complete and backed by recorded verification evidence.",
    "done": "Closed and archived; retained only for dependency context."
  },
  "features": [
    {
      "id": "repo-harness-simplification",
      "priority": 0,
      "area": "harness",
      "title": "Lean repo harness",
      "user_visible_behavior": "A new agent can start from AGENTS.md, feature_list.json, and current-state.md without loading the whole harness history.",
      "status": "passing",
      "dependencies": [],
      "verification": [
        "Root startup instructions name only the three always-read files",
        "Harness docs preserve the five-subsystem model and use on-demand routing",
        "Feature and state files are compact enough for every-session reading",
        "JSON and diff checks pass"
      ],
      "evidence": [
        "2026-06-17: Startup, harness skill routing, harness overview, active feature state, current-state handoff, README development guidance, and affected harness docs were compressed to the lightweight five-subsystem model. JSON validation, local Markdown link check, pin policy, stale-reference scans, and git diff checks passed.",
        "2026-06-24: Harness gap-analysis pass against the learn-harness-engineering course closed the remaining missed mechanisms: a status_legend plus a single `active` slice make single_active_feature checkable; a startup baseline gate, decision-rationale capture, boundary-journey table, independent evaluator routing for security changes, agent-oriented-error and verify-before-refactor gates, a representative task set for cleanup decisions, and a mechanical Tauri capability-scope guard test. Tauri tests (incl. the capability guard), pin check, JSON validation, and git diff checks passed."
      ],
      "notes": "Historical evidence remains in docs/harness/evidence/evidence-log.md and product docs. It is not startup context."
    },
    {
      "id": "tauri-alpha-launch-feedback",
      "priority": 1,
      "area": "ui",
      "title": "Tauri alpha launch feedback",
      "user_visible_behavior": "The alpha desktop shell supports setup, dashboard, profile selection, folder picking, run-plan review, confirmed launch, image readiness, sanitized live status, and opt-in bounded raw output snapshots.",
      "status": "passing",
      "dependencies": [
        "rust-cli-conversion",
        "runtime-hardening-macos-only-boundary",
        "run-observability"
      ],
      "verification": [
        "Typed Rust commands and narrow Tauri capabilities cover the exposed UI paths",
        "Frontend and Tauri tests cover launch, readiness, status, and log snapshot behavior",
        "Raw logs stay hidden by default and are not durably stored in frontend state"
      ],
      "evidence": [
        "2026-06-16: Tauri/Svelte scaffold, launch execution, image readiness, resource warnings, live status, raw-log design, and bounded log snapshot slices passed focused Rust, Tauri, frontend, Playwright, and Apple container checks. See docs/harness/evidence/evidence-log.md for command detail."
      ],
      "notes": "This compact row replaces the previous per-slice startup ledger. Detailed slice history remains in the evidence log."
    },
    {
      "id": "tauri-stop-run-control",
      "priority": 1,
      "area": "ui",
      "title": "Tauri stop run control",
      "user_visible_behavior": "The desktop app can stop one validated active RunHaven run only after explicit user confirmation.",
      "status": "passing",
      "dependencies": [
        "tauri-alpha-launch-feedback"
      ],
      "verification": [
        "`stop_run` uses a typed request and response",
        "Rust validates the run id, active marker, and RunHaven-owned container before mutation",
        "The command lives behind a narrow run-control capability",
        "Frontend state does not imply success before the command returns",
        "Focused Rust, Tauri, frontend, and Playwright checks pass"
      ],
      "evidence": [
        "2026-06-25: Completed the v1 desktop maintainability split (milestone 1 / V1-G10) as the prerequisite before adding GUI controls. Split src-tauri/src/commands/mod.rs (528 -> 342) into validation.rs and warnings.rs; split ui/src/commands/runhaven.ts (543) into types.ts/client.ts/plan.ts behind a barrel; split ui/src/app/App.svelte (569 -> 409) into setup/plan-review/last-launch/run-status/run-output panel components. Behavior preserved; Tauri tests (19)/clippy, svelte-check, 15 unit tests, build, and Playwright e2e (2) pass.",
        "2026-06-25: Implemented stop_run. A shared library core `stop_active_run` validates the run id, active marker, and RunHaven-owned container before calling `container stop`, and backs both the CLI `runs_stop` and the typed Tauri `stop_run` command (StopRunRequest/StopRunResponse). stop_run requires confirm_stop, lives behind the narrow `run-control` capability (`allow-stop-run`, added to build.rs and run-control.json; capability_guard confirms it is an allow-* scope), and the RunStatusPanel shows a confirm checkbox plus Stop button whose success state is set only after the command returns. Verified: main `cargo test` (49), Tauri `cargo test` (23, incl. 4 new stop_run tests + capability_guard)/clippy, svelte-check, 15 unit tests, build, and Playwright e2e (2, extended to stop a preview run)."
      ],
      "notes": "First GUI implementation slice after the CLI-complete v0.5.0 scope is closed or explicitly accepted. Add broader run recovery, image, diagnostics, cleanup, and worktree review as separate GUI slices."
    },
    {
      "id": "tauri-kill-repair-run-control",
      "priority": 1,
      "area": "ui",
      "title": "Tauri kill and repair run control",
      "user_visible_behavior": "The desktop app can hard-stop a validated active run and clear a stale active marker, each only after explicit user confirmation. Completes the V1-G3 desktop run-control surface (stop, kill, repair).",
      "status": "passing",
      "dependencies": [
        "tauri-stop-run-control"
      ],
      "verification": [
        "`kill_run` and `repair_run` use typed requests/responses and require explicit confirmation",
        "Rust validates the run id, active marker, and RunHaven-owned container before mutation",
        "Both commands live behind the narrow run-control capability (`allow-kill-run`, `allow-repair-run`)",
        "Frontend state does not imply success before the command returns",
        "Focused Rust, Tauri, frontend, and Playwright checks pass"
      ],
      "evidence": [
        "2026-06-25: Implemented kill_run and repair_run mirroring stop_run. Shared library cores kill_active_run and repair_active_run validate the run id, active marker, and RunHaven-owned container before container kill / stale-marker repair, and back both the CLI runs_kill/runs_repair and the typed Tauri commands. Both require confirmation and sit behind the run-control capability (allow-kill-run, allow-repair-run added to build.rs and run-control.json; capability_guard confirms allow-* scope). RunStatusPanel gained Hard stop and Repair marker controls with per-action confirm checkboxes over shared control state; success messages set only after the command returns. Verified: main cargo test (49), Tauri cargo test (27, incl. 4 new kill/repair tests + capability_guard)/clippy, svelte-check, 15 unit tests, build, and Playwright e2e (2, extended to stop+kill+repair a preview run). Completes V1-G3."
      ],
      "notes": "Run-control mutations (stop/kill/repair) all share one validated library core per action and the narrow run-control capability. Image, state/network cleanup, diagnostics, and worktree review remain separate GUI slices."
    },
    {
      "id": "tauri-diagnostics",
      "priority": 1,
      "area": "ui",
      "title": "Tauri secret-free diagnostics",
      "user_visible_behavior": "The desktop app shows provider egress decisions and auth broker status/log for RunHaven runs, read-only and without secrets or workspace paths.",
      "status": "passing",
      "dependencies": [
        "tauri-alpha-launch-feedback"
      ],
      "verification": [
        "get_egress_log, get_auth_log, and get_auth_status are read-only and return only metadata (no secrets, no workspace paths)",
        "Commands live behind the read-only main-read capability",
        "Structured responses map from existing JSONL records and the auth status payload",
        "Focused Rust, Tauri, frontend, and Playwright checks pass"
      ],
      "evidence": [
        "2026-06-25: Added read-only get_egress_log, get_auth_log, and get_auth_status behind the main-read capability. read_egress_policy_log/read_auth_broker_log back the logs; a new shared auth_status_payload core backs both the CLI auth status and the Tauri command. Responses map only metadata fields (host/port/decision/reason/count, method/path/upstream-status) and intentionally omit workspace paths, mirroring the CLI text output. A self-contained DiagnosticsPanel fetches and renders them on demand, keeping App.svelte lean. Verified: main cargo test (49), Tauri cargo test (30, incl. 3 diagnostics mapper tests + capability_guard)/clippy, svelte-check, 15 unit tests, build, and Playwright e2e (3, incl. a diagnostics load test)."
      ],
      "notes": "Partial V1-G5: egress log and auth status/log are exposed. why host/workspace/network/state explanations, blocked-host review, and auth explain remain separate GUI slices."
    },
    {
      "id": "cli-complete-v0.5.0",
      "priority": 0,
      "area": "cli",
      "title": "CLI-complete v0.5.0 release",
      "user_visible_behavior": "By v0.5.0, all intended CLI product behavior is complete, documented, and verified. Later v1 work focuses on the first-class desktop app without expanding CLI scope except bug fixes, security fixes, release pin updates, documentation corrections, and internal GUI support that preserves CLI semantics.",
      "status": "passing",
      "dependencies": [],
      "verification": [
        "All planned CLI product commands and user docs are complete",
        "Secure CLI defaults are the easiest path, with supported lower-security choices explicit and warned",
        "Stable CLI output, JSON, and local data lifecycle decisions are recorded",
        "CLI crates and modules remain cohesive with no known large-file or duplication debt deferred to v1",
        "Runtime, provider, auth, worktree, cleanup, and SSH fail-closed checks pass",
        "Profile support tiers are recorded from CLI evidence",
        "No v1-blocking CLI feature work remains after the v0.5.0 tag"
      ],
      "evidence": [
        "2026-06-24: Closed the v0.5.0 CLI contract gaps. G4: runtime proven on macOS 27.0 (doctor green for all pins; full provider+ssh smoke passed). G1: documented --user and the runs attach --user/--workdir/--tty overrides. G2: documented runs/egress/auth JSONL records as best-effort/pre-stable, append-only, metadata-only, with --json as the supported read path. G3: published the per-profile support matrix. G5: confirmed --ssh fail-closed across docs, behavior, and tests with no raw-key workaround language. G6: added plain-language security notices on stderr for every lower-security run choice (internet default, --env, custom/root --user, --provider-host, --allow-sensitive-workspace, --image), computed once in build_run_plan; focused tests cover present-for-lower-security and absent-for-secure. G7: touched files stay under the size guard with no new duplication (auth_broker.rs 499 and egress.rs 495 remain watched, untouched). Verified with root cargo fmt/test (47 unit + 6 integration)/clippy, Tauri test (19, 1 ignored)/clippy, and a live plan security-notice check. Tagged release notes deferred to release-readiness.",
        "2026-06-25: Resolved the network-default decision per the user directive that the secure path must be the easiest path. --network is now optional and resolves profile-aware: provider for profiles with bundled provider hosts (claude, codex, copilot, gemini) and internet for those without (shell, antigravity), where provider would be an empty allowlist. A provider-default run reaches the agent's own API but not arbitrary hosts; plan and run inform the --provider-host / --network internet escape hatch and never block. Updated CLI plus CAPABILITIES, USAGE, ARCHITECTURE, SECURITY_MODEL, README, V1_RELEASE_PLAN, and security-boundary-map; added a focused default_network_mode test. Verified with cargo fmt/test (48 unit + 6 integration)/clippy, Tauri test (19, 1 ignored)/clippy, and live plan checks (claude defaults to provider, shell to internet, explicit --network internet override works).",
        "2026-06-25: Produced documented evidence that every CLI surface is tested. Added scripts/cli_surface_check.sh, a repeatable live breadth check over every command family (39/39 surfaces on macOS 27.0), complementing scripts/apple_container_smoke.sh for the deep provider-egress and SSH boundary; coverage indexed in docs/CLI_SURFACE_COVERAGE.md. The surface check caught and fixed a real bug: the GitSnapshot/GitChange `available` serde tag serializes as the string \"true\"/\"false\" but four sites read it with Value::as_bool (always None), silently breaking run --worktree, runs diff, and runs merge for every clean repo. Fixed with a canonical git_value_available reader plus typed enum matches and a regression test. Final evidence on macOS 27.0: cli_surface_check.sh 39/39, smoke --with-provider --with-ssh passed, both crates green on fmt/test (49 unit + 6 integration)/clippy plus pins, JSON, and diff."
      ],
      "notes": "Durable scope is tracked in docs/V1_RELEASE_PLAN.md; open gaps are tracked in docs/RELEASE_GAP_ANALYSIS.md. RunHaven remains alpha/pre-release until after this milestone. After v0.5.0, keep CLI changes contract-preserving unless a security or bug-fix exception is explicit. Must not change while active: the default security boundary (no host home, credential, or raw SSH key mount; non-root images; provider egress allowlist) and existing stable CLI output and JSON contracts."
    },
    {
      "id": "runtime-security-hardening",
      "priority": 1,
      "area": "runtime",
      "title": "Apple container runtime and security hardening",
      "user_visible_behavior": "The Apple `container` boundary (task-scoped runs, provider egress, image lifecycle, state/network cleanup) is re-verified on the current host and audited for correctness and security gaps, with real findings fixed under test, before any new product surface.",
      "status": "passing",
      "dependencies": [
        "cli-complete-v0.5.0"
      ],
      "verification": [
        "Recurring runtime evidence gates pass on macOS 26+ (scripts/apple_container_smoke.sh --with-provider --with-ssh and runhaven doctor)",
        "A focused security/correctness audit of src/runhaven/runtime, provider, and image is recorded against docs/APPLE_CONTAINER_GAP_ANALYSIS.md",
        "Real findings are fixed in focused slices with regression tests; deferred items are recorded with reason and risk",
        "No default security boundary is weakened (no host home, credential, or raw SSH key mount; non-root images; provider egress allowlist)"
      ],
      "evidence": [
        "2026-06-26: Ran a two-lens security/correctness audit of the Apple container boundary (apple-container-expert + security-audit, cross-referenced against the upstream Apple source clone at /Users/c/Documents/GitHub/apple-container). Core boundary verified sound: non-root agent, read-only rootfs, cap-drop ALL on the agent run, single workspace+state mounts, explicit env only, fail-closed validation, canonicalized path checks (no ../ or symlink bypass), exact/dot-boundary allowlist, SSRF/DNS-rebind defense, hostOnly network replacement (not additive), provider teardown on error paths, run-control ownership validation, JSON fail-closed, secrets absent from all logs. Empirically confirmed finding #1 (Medium) with a scoped live probe on macOS 27.0: a hostOnly guest opened raw TCP to a host listener on the gateway while direct internet egress was refused; Apple container 1.0.0 has no per-port guest-to-host firewalling, so it is remediated by a SECURITY_MODEL.md caveat + guidance with an in-guest eBPF filter logged design-first in NON_UI_BACKLOG.md.",
        "2026-06-26: Fixed the directly-fixable findings under test. #2: the short-lived state-volume-prep root container now sets --cap-drop ALL and re-adds only CHOWN/FOWNER/DAC_OVERRIDE (verified against upstream RuntimeService.effectiveCapabilities that drop-ALL-then-add yields exactly those three; live smoke confirmed volume prep still succeeds). #4: added /usr,/bin,/sbin,/opt to sensitive_workspace_paths so they require --allow-sensitive-workspace. #5: the provider proxy and Codex broker now warn on the 0.0.0.0 bind fallback (off-subnet clients are still rejected). Documented #1 and the broker usable-not-readable / suffix-wildcard notes (#6) in SECURITY_MODEL.md. Verified: cargo fmt, cargo test --locked (52 unit incl. 3 new + 6 integration), clippy -D warnings, git diff --check, and scripts/apple_container_smoke.sh on macOS 27.0. Work is on branch runtime-security-hardening."
      ],
      "notes": "Led the near-term roadmap per the 2026-06-26 directive that defers all GUI/UI work to the very end. Used the upstream Apple source checkout at /Users/c/Documents/GitHub/apple-container/ as source-map evidence. Design-first follow-ups now live in NON_UI_BACKLOG.md: proxy-side credential injection for non-Codex providers (#3) and the in-guest eBPF egress filter for finding #1. The P2 SSH fail-closed item is tracked separately as ssh-forwarding-boundary and stays blocked pending an upstream non-root socket fix."
    },
    {
      "id": "multi-provider-broker",
      "priority": 1,
      "area": "provider",
      "title": "Multi-provider API-key credential broker",
      "user_visible_behavior": "The host-side API-key broker covers Codex, Claude, and Gemini via --api-key-broker-env: the real key stays host-side and the guest is redirected at the broker with only a placeholder. Copilot stays design-only; OAuth and subscription logins use isolated in-container state.",
      "status": "passing",
      "dependencies": [
        "runtime-security-hardening"
      ],
      "verification": [
        "The broker is provider-agnostic (ProviderBrokerProfile: upstream host, path matcher, credential-injection strategy, guest redirect) with explicit Codex, Claude, and Gemini profiles",
        "The real host key never enters the guest command or environment for any provider; the guest receives only a placeholder plus a base-URL redirect",
        "Copilot has no broker profile (token exchange + dynamic API host); auth_profiles records the blocker",
        "Focused tests cover per-provider injection, path matching, and guest config; Codex behavior preserved; main and Tauri checks pass",
        "Docs (AUTH_BROKER, SECURITY_MODEL, ARCHITECTURE, CAPABILITIES, USAGE) reflect the multi-provider broker, the --api-key-broker-env flag, and the OAuth-vs-API-key distinction"
      ],
      "evidence": [
        "2026-06-26: Generalized the Codex API-key broker into a provider-agnostic core and wired Codex, Claude, and Gemini brokers into the run orchestration. ProviderBrokerProfile drives upstream host, path matcher, credential injection (Bearer / x-api-key+anthropic-version / x-goog-api-key), and guest redirect (Codex custom-provider config with /v1; Claude/Gemini ANTHROPIC_BASE_URL / GOOGLE_GEMINI_BASE_URL). Renamed --codex-api-key-broker-env to --api-key-broker-env (old name kept as alias) and the field/types accordingly; flipped Claude/Gemini auth_profiles from design-only to api-key-broker; Copilot stays design-only (cannot be brokered without TLS interception). Verified: cargo fmt, cargo test --locked (58 lib incl. 3 new broker-config tests + 6 integration), clippy -D warnings, Tauri cargo test (30) and clippy. Claude/Gemini live redirect needs a real provider key on the target CLI version (unit + fail-closed tested here). Branch runtime-security-hardening, commits 4f11716, b5cf193, 2d696e2 plus docs."
      ],
      "notes": "OAuth and subscription logins are out of broker scope and use isolated in-container state; RunHaven never reads host ~/.claude.json or Keychain. A host-side OAuth-token broker was researched 2026-06-26 and decided against (provider terms forbid relaying subscription/OAuth credentials, subscription tokens are not drop-in bearers, and it would cross the read-host-creds boundary); the isolated-login UX is the follow-up candidate in NON_UI_BACKLOG.md. Canonical broker doc: docs/AUTH_BROKER.md."
    },
    {
      "id": "oauth-isolated-login",
      "priority": 1,
      "area": "provider",
      "title": "Easy OAuth isolated login",
      "user_visible_behavior": "OAuth and subscription logins (Claude Pro/Max, ChatGPT, Google, GitHub) work by logging in once inside the sandbox with no host credentials mounted. --auth-scope agent (default) shares one login per agent across all projects so you log in once; --auth-scope project isolates it per workspace.",
      "status": "active",
      "dependencies": [
        "multi-provider-broker"
      ],
      "verification": [
        "--auth-scope agent produces one shared per-agent state volume; project keeps per-workspace isolation",
        "The shared-login security tradeoff is documented and the per-project opt-out is available",
        "OAuth login is verified to work and persist inside a RunHaven sandbox",
        "Focused Rust, Tauri, and live-runtime checks pass"
      ],
      "evidence": [
        "2026-06-26: Live-verified that a Claude Max subscription OAuth login works end to end inside a RunHaven sandbox (provider mode; login hosts api.anthropic.com/claude.ai/platform.claude.com allowlisted; no host creds mounted). Telemetry/registry hosts (Datadog, raw.githubusercontent.com, registry.npmjs.org) were correctly blocked and the agent still ran. Added --auth-scope <agent|project> (default agent): agent shares one per-agent home volume (runhaven-<agent>-shared-home) across all workspaces so the login is done once; project keeps the per-workspace volume. Documented the shared-login tradeoff in SECURITY_MODEL/USAGE/CAPABILITIES. Verified: cargo fmt, cargo test --locked (59 lib incl. a new auth-scope test + 6 integration), clippy -D warnings, Tauri test (30) and clippy."
      ],
      "notes": "Built: once-per-agent --auth-scope (Claude verified live); `runhaven login claude` (setup-token opt-in; live-verified 2026-06-26 zero-friction); and `runhaven login` for Codex (`codex login --device-auth`) and Copilot (`copilot login`) as in-sandbox device flows on the shared home volume, with login hosts allowlisted (auth.openai.com; github.com + api.github.com). Codex/Copilot are live-verified 2026-06-26 (codex login reached auth.openai.com and succeeded; copilot login reached github.com/login/device and succeeded; both persisted). Verified gotchas: Codex needs the ChatGPT account device-code-authorization setting; Copilot prompts to store the token in a plaintext config file (default N) and the user must answer y (token lands in the isolated volume). Antigravity login built + live-verified 2026-06-26: `runhaven login antigravity` runs agy (first-run Google OAuth, /exit after); the model answered prompts. Pinned 4 hosts from observed egress (oauth2/www.googleapis.com, cloudcode-pa + daily-cloudcode-pa.googleapis.com); accounts.google.com/antigravity.google are browser-side. agy's eligibility check (lh3.googleusercontent.com) is cosmetic and left out (opt-in flag documented); antigravity now defaults to provider mode. Decision: tight pins, not *.googleapis.com (storage exfil risk). All four of the user's agents (Claude/Codex/Copilot/Antigravity) now verified. The user flagged friendlier UX/phrasing as the next thread (image-not-built message instead of a registry 401, Copilot keychain heads-up, Codex toggle heads-up) and asked about geo/endpoint variation of the cloudcode-pa hosts (candidate: a narrow *-cloudcode-pa.googleapis.com matcher). Remaining: that consolidated UX pass (its hosts are reverse-engineered, so observe real egress in-sandbox before pinning an allowlist) to complete the user's Claude/Codex/Antigravity/Copilot set; Gemini stays on the API-key broker. The per-run gateway-bind warning is quieted (the 0.0.0.0 fallback is the expected, subnet-filter-protected path on this runtime). Optional: auto-opening the device URL on the host (needs stdout capture) and a one-time shared-login notice on first volume creation. Tracked in docs/NON_UI_BACKLOG.md."
    },
    {
      "id": "desktop-first-class-v1",
      "priority": 4,
      "area": "ui",
      "title": "First-class desktop surface (deferred to roadmap end)",
      "user_visible_behavior": "The desktop app is the primary safe path for setup, image readiness and rebuild, planning, launch, live status, bounded output, stop, kill, repair, diagnostics, worktree review, and safe cleanup.",
      "status": "planned",
      "dependencies": [
        "cli-complete-v0.5.0",
        "tauri-stop-run-control",
        "runtime-security-hardening",
        "release-readiness"
      ],
      "verification": [
        "Tauri commands remain typed, bounded, and capability-scoped",
        "The desktop app covers the complete beginner-safe workflow without requiring CLI knowledge",
        "The secure path is the easiest default and supported lower-security choices are explicit, warned, and recoverable",
        "Frontend, Tauri, and shared planner modules remain cohesive with duplication removed during each slice",
        "Every mutating desktop operation previews the exact target and requires explicit confirmation",
        "Frontend, Tauri, accessibility, visual, and Apple container smoke checks pass",
        "The release artifact is signed, notarized, checksummed, and backed by provenance"
      ],
      "evidence": [],
      "notes": "Deferred to the very end of the roadmap per the 2026-06-26 directive that pushes all GUI/UI work last; runtime/security hardening, remaining non-UI product scope, and a CLI-based public release come first. Shipped GUI slices (tauri-stop-run-control, tauri-kill-repair-run-control, tauri-diagnostics) stay passing and are re-sequenced, not reverted. The version label is no longer locked to v1.0.0. Durable scope is tracked in docs/V1_RELEASE_PLAN.md; open gaps are tracked in docs/RELEASE_GAP_ANALYSIS.md. Keep each command family in separate slices. Apple container machine is not the default boundary, but explicit or user-managed machine workflows should warn and require intent rather than be blocked solely because they are less secure."
    },
    {
      "id": "ssh-forwarding-boundary",
      "priority": 1,
      "area": "security",
      "title": "SSH forwarding fails closed",
      "user_visible_behavior": "`--ssh` refuses before launch until Apple container forwarding is proven usable without raw key mounts or root default user changes.",
      "status": "blocked",
      "dependencies": [],
      "verification": [
        "Planner and run paths refuse `--ssh`",
        "No raw SSH key mount is introduced",
        "A no-secret non-root Apple container smoke is required before unblocking"
      ],
      "evidence": [
        "2026-06-16: Apple container 1.0.0 exposed the forwarded socket to the non-root guest user, but `ssh-add -l` returned permission denied. RunHaven fails closed on `--ssh`."
      ],
      "notes": "Do not re-enable without current primary-source/runtime evidence and focused security review."
    },
    {
      "id": "release-readiness",
      "priority": 2,
      "area": "release",
      "title": "Pre-release readiness (CLI-based public release)",
      "user_visible_behavior": "Before a public release, local verification, Apple container smokes, dependency pins, docs, and security evidence are current.",
      "status": "planned",
      "dependencies": [
        "cli-complete-v0.5.0",
        "runtime-security-hardening"
      ],
      "verification": [
        "`./init.sh` passes on macOS 26+",
        "`runhaven doctor` and relevant Apple container smokes pass",
        "Pinned dependency and runtime evidence is current",
        "Secure-easy defaults, maintainability review, dependency pinning, and harness state are current",
        "Security, installation, usage, and release docs are aligned"
      ],
      "evidence": [],
      "notes": "Keep release work separate from feature slices. RunHaven remains alpha/pre-release until after v0.5.0. Per the 2026-06-26 directive, this gates a CLI-based public release that comes before the deferred desktop/TUI work; the desktop release is a later, separate milestone (desktop-first-class-v1). Open gaps are tracked in docs/RELEASE_GAP_ANALYSIS.md; the durable release contract in docs/V1_RELEASE_PLAN.md is superseded on sequencing by this directive."
    }
  ]
}
