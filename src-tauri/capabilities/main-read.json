{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-read",
  "description": "Read-only access for RunHaven setup, dashboard, profile, image status, run status, and planning commands.",
  "windows": ["main"],
  "permissions": [
    "allow-get-setup-status",
    "allow-list-agents",
    "allow-get-dashboard-status",
    "allow-get-image-status",
    "allow-get-run-status",
    "allow-plan-run"
  ]
}
