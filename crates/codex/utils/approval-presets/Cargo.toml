[package]
name = "codex-utils-approval-presets"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
codex-protocol = { path = "../../protocol" }

[lib]
test = false
doctest = false
