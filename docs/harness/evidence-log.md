# Evidence Log

Use this for compact current evidence. Keep raw logs out of this file.

| Date | Scope | Command Or Review | Result | Notes |
| --- | --- | --- | --- | --- |
| 2026-06-14 | Harness initialization | `harnessforge init --target .` | passed | Existing `AGENTS.md` was preserved; missing harness files were created. |
| 2026-06-14 | Harness audit | `PYTHONPATH=../HarnessForge/src python3 -m harnessforge audit --target . --min-score 85` | passed | Reported 100/100 after the macOS-only correction. |
| 2026-06-14 | POSIX entrypoint | `PYTHON=<temporary-venv-python> ./init.sh` | passed | Ran compileall, unit tests, pin policy, ruff, mypy, and build. |
| 2026-06-14 | macOS-only support | source and docs review | passed | Removed the non-macOS verification entrypoint and unsupported platform claims. |
| 2026-06-14 | Project logo | `magick identify docs/assets/logo.png` plus visual inspection | passed | Tracked logo asset is a stripped 512x512 PNG used by `README.md`. |
| 2026-06-14 | Harness audit | `PYTHONPATH=../HarnessForge/src python3 -m harnessforge audit --target . --min-score 85` | passed | Current score is 100/100 after removing non-macOS verification surfaces. |
| 2026-06-14 | RunHaven rename | source checks, temporary-venv static checks, build, wheel smoke, no-ignore old-name scan, harness audit | passed | Package, module, command, image tags, docs, tests, and harness state use RunHaven/`runhaven`; ignored local virtualenvs were removed because they encoded stale checkout paths. |
| 2026-06-14 | Runtime hardening and macOS-only boundary | unit tests, static checks, build, `./init.sh`, harness audit, `runhaven doctor`, `runhaven state list`, internal-network smoke, and `runhaven plan` smoke | passed | Command validation, unsafe overrides, TTY controls, state commands, host-only internal network creation, and macOS 26+ only verification are covered. |
| 2026-06-14 | Follow-up hardening pass | focused unit tests, full unit suite, and pin check | passed | Added root group rejection, parser help cwd safety, dynamic image template pin discovery, and run/doctor edge-case coverage. |
| 2026-06-14 | Cleanup pass | stale-reference scan, pin check, JSON validation, diff check, and HarnessForge audit | passed | Removed stale local paths, stale local-venv evidence, and old HarnessForge predecessor references from tracked docs. |
| 2026-06-14 | Second follow-up hardening pass | sandboxed Antigravity audit, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, help/plan/doctor smokes, HarnessForge audit, cleanup scans | passed | Added fail-closed network mode validation, leading-zero root identity rejection, sensitive macOS system path blocking, doctor remedies, agent-argument help, and macOS-only pin-ledger enforcement. |
| 2026-06-14 | Provider egress preparation | Playwright-rendered Apple DocC review, complete user-supplied DocC snapshot review, focused fail-closed tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, doctor smoke, and HarnessForge audit | passed | Added reserved `--network provider` mode that failed closed at that stage, explicit plan egress status, and docs stating internet mode remained unrestricted until enforcement was proven. The complete snapshot covered 1,022 rendered Markdown pages plus raw JSON with zero fetch failures and no exact hits for egress or allowlist control terms. |
| 2026-06-14 | Provider egress proxy smoke | allowlist proxy unit tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, default host live smoke, and `api.openai.com` live smoke | passed | Proved the host CONNECT proxy pattern on an internal Apple `container` network: allowed proxied HTTPS succeeded; denied proxied host, proxied IP literal, direct DNS, and direct IP paths failed. At that stage, normal `--network provider` runs remained fail-closed until lifecycle integration landed. HarnessForge audit was skipped for this pass by user instruction while the sibling repo was being worked on. |
| 2026-06-14 | Provider wording cleanup | focused CLI tests, ruff, mypy, and diff check | passed | Aligned CLI help and roadmap wording with the smoke-proven but not yet normal-run-integrated provider proxy state. |
| 2026-06-14 | Provider runtime integration | focused lifecycle tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, and live `runhaven run shell --network provider --provider-host example.com` smoke | passed | Integrated the proxy lifecycle into normal provider runs. The live run allowed proxied HTTPS and denied proxied host, proxied IP literal, direct DNS, and direct IP paths; cleanup left no provider network or test state volume. |
| 2026-06-14 | Provider host guard cleanup | focused planner and CLI tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, ruff, and mypy | passed | Rejected single-label provider hosts such as `com` so explicit provider additions must be fully qualified. Docs now state that a listed host permits that host and its subdomains. |
| 2026-06-14 | Supplemental Apple container source review | direct web source review and Antigravity research pass | passed | Reviewed Apple Open Source, DeepWiki, Wikipedia, The Register, HowToUseLinux, Apidog, and Suraj Deshmukh references. Added durable research notes and reinforced that RunHaven should avoid `container machine` home-mount defaults. |
| 2026-06-14 | Provider blocked-host diagnostics | focused egress, CLI, and plan tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, and live `runhaven run shell --network provider --provider-host example.com` diagnostic smoke | passed | Provider proxy denials are recorded as capped unique host/port pairs and summarized after the run. The live smoke reported denied `iana.org:443` and cleanup left no provider network or test state volume. |
| 2026-06-15 | Provider diagnostics and DNS guard | focused proxy and CLI tests, manual `why host` and `egress log` checks, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Provider proxy now rejects allowed hosts that resolve to non-public addresses, aggregates and persists provider policy decisions, and exposes first-pass `runhaven why host ...` diagnostics. |
| 2026-06-15 | Provider endpoint matrix | provider source review, pinned package scan, focused CLI/plan tests, full unit suite, ruff, mypy, pin check, diff check, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added structured provider endpoint metadata, source-backed bundled auth and routing hosts, explicit-review endpoint guidance in `runhaven why host`, and `docs/PROVIDER_ENDPOINTS.md`. |
| 2026-06-15 | Grouped blocked-host review and profile smoke | focused CLI and smoke-script tests, full unit suite, ruff, mypy, pin check, diff check, and live `scripts/provider_egress_smoke.py --agent codex` | passed | Provider runs now print grouped blocked-host review with run id, reason, count, rule, and suggested next action. The smoke harness can test bundled profile hosts; the Codex smoke allowed proxied `api.openai.com` and `chatgpt.com` while blocking denied host, proxied IP literal, direct DNS, and direct IP paths. |
| 2026-06-15 | Auth broker boundary diagnostics | focused auth CLI tests, manual `auth status` and `auth explain codex` smokes, provider auth source review, docs review, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added static secret-free auth broker metadata, `runhaven auth status`, `runhaven auth explain AGENT`, JSON output, and `docs/AUTH_BROKER.md`; no credential store or environment values are read. |
| 2026-06-15 | Empty-allowlist regression coverage | focused planner and egress tests plus `PYTHON=<temporary-venv-python> ./init.sh` | passed | Internet mode is explicitly unrestricted with no provider allowlist, internal mode is local-only with no provider allowlist, provider mode fails closed with an empty allowlist, and `EgressPolicy` rejects empty allowlists. |
| 2026-06-15 | Codex API-key broker prototype | focused planner/proxy/CLI tests, manual broker plan/dry-run/missing-env/auth smokes, official Codex config source review, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added opt-in `--codex-api-key-broker-env NAME` for Codex provider runs. The host broker keeps the raw API key out of the planned command and guest environment, restricts clients to the provider subnet, and forwards only Responses API create requests. |
| 2026-06-15 | Codex broker observability and smoke harness | focused broker/log/smoke tests, full unit suite, ruff, mypy, pin check, diff check, manual `auth log`, no-key smoke skip, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added sanitized broker decisions, `runhaven auth log`, secret-free auth broker JSONL entries, and optional `scripts/codex_broker_smoke.py`. The live provider request was skipped because no disposable API key was set. |
| 2026-06-15 | Run observability | red/green focused CLI tests, full CLI suite, manual `runs list/show` smoke, ruff, mypy, pin check, JSON validation, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runs.jsonl`, `runhaven runs list`, and `runhaven runs show RUN_ID`. Run records summarize provider policy, auth broker, and cleanup outcomes without command lines, agent arguments, env names, env values, request bodies, or token values. |
| 2026-06-15 | Joined run logs | red/green focused CLI tests, full CLI suite, manual `runs log` text/JSON smoke, ruff, mypy, pin check, JSON validation, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs log RUN_ID` to join the run record with matching provider policy and auth broker entries for the same run id. Text and JSON output remain secret-free. |
| 2026-06-15 | Git run metadata | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, manual `runs show/log` git smoke, cleanup scan, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Run records now include git repo root, before and after `HEAD`, dirty state, changed count, and capped relative paths for git workspaces. Records still omit diffs, file contents, prompts, commands, env details, request bodies, and token values. |
| 2026-06-15 | Live run diffs | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, manual `runs diff` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs diff RUN_ID`. It prints live git diff output for committed, tracked dirty, untracked dirty, and mixed committed-and-dirty changes only after recorded repo root, `HEAD`, dirty state, changed count, and path set still match the workspace, and refuses stale or unavailable metadata. |
| 2026-06-15 | Active run stop | red/green focused CLI and plan tests, full unit suite, ruff, mypy, pin check, manual `runs stop` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added named RunHaven containers, secret-free active-run markers, `runhaven runs stop RUN_ID`, and stopped status for runs that exit after a stop request. |
| 2026-06-15 | Active run discovery | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs active` text/JSON smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs active` to list current active-run markers without command lines, agent arguments, env details, prompts, request bodies, token values, or Apple `container` CLI access. |
| 2026-06-15 | Active run attach | red/green focused CLI tests, local Apple `container exec --help`/`container attach --help` review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs attach` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs attach RUN_ID` to open a guarded `container exec` shell or command in an active RunHaven-owned container. |
| 2026-06-15 | Active run log follow | red/green focused CLI tests, local Apple `container logs --help` review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs logs-follow` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs logs-follow RUN_ID` to follow recent and live logs for an active RunHaven-owned container. |
| 2026-06-15 | Active run status | red/green focused CLI tests, local Apple `container inspect --help`/JSON review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs status` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs status RUN_ID` to show sanitized active marker plus live container state without raw inspect arguments, environment, or mount fields. |
| 2026-06-15 | Active run kill | red/green focused CLI tests, local Apple `container kill --help` review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs kill` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs kill RUN_ID` as the explicit hard-stop recovery path for an active RunHaven-owned container and record killed runs as `killed`. |
| 2026-06-15 | Active run repair | red/green focused CLI tests, local Apple missing-container inspect review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs repair` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs repair RUN_ID` to remove a stale active marker only after Apple `container inspect` reports the recorded RunHaven-owned container is not found. |
| 2026-06-15 | Bulk active run repair | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs repair --all` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs repair --all` to inspect valid active markers and remove only those whose recorded RunHaven-owned container is confirmed missing. |
| 2026-06-15 | Repair JSON output | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual repair JSON smokes, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `--json` to `runhaven runs repair RUN_ID` and `runhaven runs repair --all` with secret-free results, counts, and exit codes. |
| 2026-06-15 | Guided setup | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runhaven setup --agent shell` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added non-mutating `runhaven setup` to run prerequisite checks, print exact fixes, and show profile-specific first-run commands. |
| 2026-06-15 | Setup network guidance | red/green focused CLI test, focused setup/doctor tests, full unit suite, compileall, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runhaven setup --agent shell` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added goal-based network guidance to `runhaven setup` for local-only, provider-only, package install, and unrestricted internet runs. |

Rules:

- Record command name, scope, result, and risk.
- Do not paste secrets, local absolute paths, or long command output.
- Prefer one current row per meaningful verification event.
