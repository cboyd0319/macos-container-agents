# Evidence Log

Use this for compact current evidence. Keep raw logs out of this file.

| Date | Scope | Command Or Review | Result | Notes |
| --- | --- | --- | --- | --- |
| 2026-06-24 | Repo-wide docs accuracy audit | Four-cluster audit of all 54 tracked Markdown files against canonical state, manifests, and source; repo-wide relative Markdown link check (0 broken); repo-harness path-resolution check; `runhaven-check-pins`; JSON validation; and `git diff --check` | passed | Root/core-product/most harness docs already accurate. Fixed `docs/RESEARCH.md` (dated the stale "current image pins" line, added 2026-06-24 current-pins note + no-workflows Actions qualifier), `docs/harness/state/modularization-plan.md` (removed dead `cli/lock.rs` pointer; locking lives in `runtime/lock.rs`; refreshed largest-file counts), and `.agents/skills/harness/references/repo-harness.md` (relative paths were one level too shallow). |
| 2026-06-24 | Dependency pin audit (glib alert) | `cargo update -p time --precise 0.3.51` (root + src-tauri), `npm@11.17.0 install --package-lock-only` for ui + 4 images, root and Tauri `cargo fmt`/`test`/`clippy`/`build`, `runhaven image build {claude,codex,copilot,gemini} --dry-run`, `npm ci`/`check`/`test`/`build`/`test:e2e`/`tauri:build`, `runhaven-check-pins`, JSON validation, and `git diff --check` | passed | Audited every Cargo/npm/image-CLI/base-image/Debian pin against live upstream. Hard-pinned everywhere; no GitHub Actions present. Brought 10 pins to latest stable (`time` 0.3.51; ui `@lucide/svelte` 1.21.0, `svelte` 5.56.4, `@playwright/test` 1.61.1, `@tauri-apps/cli` 2.11.3, `svelte-check` 4.7.1, `vite` 8.1.0; bundled Claude Code 2.1.190, Codex 0.142.0, Copilot 1.0.64, Gemini CLI 0.47.0 with regenerated integrity). Dismissed Dependabot `glib` alert as not-affected (macOS-only; Linux-GTK transitive dep absent from the macOS build graph, capped at 0.18.x). |
| 2026-06-24 | Harness course gap-analysis pass | `cargo test --manifest-path src-tauri/Cargo.toml --locked` (incl. `capability_guard`), `cargo fmt --manifest-path src-tauri/Cargo.toml --check`, `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings`, `cargo run --locked --bin runhaven-check-pins`, tracked JSON validation, local Markdown link check, and `git diff --check` | passed | Closed missed mechanisms from the learn-harness-engineering review: `feature_list.json` status_legend + single `active` slice, `AGENTS.md` startup baseline gate, `current-state.md` Key Decisions, `security-boundary-map.md` boundary-journey table + independent-evaluator routing, `change-contract.md` verify-before-refactor and agent-oriented-error gates, `quality-document.md` representative task set, and a new `capability_guard` test that fails closed on any non-vetted Tauri capability scope. |
| 2026-06-18 | v0.5.0 and v1.0.0 gap analysis | CLI help smokes, Tauri command/capability scan, source file-size scan, release-doc review, pin policy, tracked JSON validation, local Markdown link check over 54 Markdown files, stale wording scans, trailing-whitespace check, and `git diff --check` | passed | Added `docs/RELEASE_GAP_ANALYSIS.md` as the active v0.5/v1 gap tracker and linked it from README, roadmap, release plan, non-UI backlog, feature state, current state, and harness routing. |
| 2026-06-18 | Release ladder and docs accuracy pass | `cargo run --locked --bin runhaven-check-pins`, tracked JSON validation, local Markdown link check over 53 Markdown files, stale alpha/pre-release/release-ladder wording scans, trailing-whitespace check, and `git diff --check` | passed | Updated active user docs, roadmap/planning docs, Tauri planning docs, and harness routing so they agree that RunHaven remains alpha/pre-release until after `v0.5.0`, `v0.5.0` is CLI-complete, and `v1.0.0` is the first-class desktop release. Historical evidence rows remain historical. |
| 2026-06-17 | Lean harness simplification and docs accuracy pass | JSON validation for all tracked JSON files, local Markdown link check over 52 tracked Markdown files, `cargo run --locked --bin runhaven-check-pins`, stale-reference scans, and `git diff --check` | passed | Compressed startup to `AGENTS.md`, `feature_list.json`, and `current-state.md`; shortened the repo harness skill, harness overview, active feature ledger, current state, manifest, roadmap, quality, first-agent archive, and related docs; updated README development guidance to point agents at the lightweight startup contract. Full `./init.sh` was skipped because no runtime code, lockfile, image, package, or Tauri capability behavior changed. |
| 2026-06-16 | Tauri raw log snapshot | `cargo test --manifest-path src-tauri/Cargo.toml --locked log_snapshot -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml --locked`, `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings`, `cargo test --locked`, `cargo clippy --all-targets --locked -- -D warnings`, `npm --prefix ui test -- --run`, `npm --prefix ui run check`, `npm --prefix ui run test:e2e`, `npm --prefix ui run build`, `scripts/apple_container_smoke.sh`, ignored live log snapshot test, desktop/mobile browser screenshot review, JSON validation, source-size guard, and `git diff --check` | passed | Added typed `get_log_snapshot` behind `run-control`, a bounded Apple `container logs -n` helper that drains stdout/stderr with fixed memory, explicit sensitive-output acknowledgement in Rust and UI, frontend preview rendering, and tests for confirmation, line cap, byte cap, invalid run id, non-RunHaven container names, preview UI, and live active-container output. Raw logs remain hidden by default, snapshot-only, outside `main-read`, and not durably stored in frontend state. |
| 2026-06-16 | Tauri raw log viewing design | Apple `container` host and `container logs --help` checks, existing CLI `runs logs-follow` inspection, Apple Container expert review, `cargo run --locked --bin runhaven-check-pins`, JSON validation, local Markdown link check, stale-text scan, and `git diff --check` | passed | Added `docs/TAURI_LOG_VIEWING_DESIGN.md` as the bounded snapshot plan for raw container stdio. The design keeps status first, requires explicit sensitive-output acknowledgement before raw logs, avoids durable frontend storage, avoids automatic redaction promises, and defers live streaming until a separate Tauri event/channel design exists. |
| 2026-06-16 | Tauri live run status | `cargo test --manifest-path src-tauri/Cargo.toml --locked run_status -- --nocapture`, `cargo fmt --manifest-path src-tauri/Cargo.toml --check`, `cargo test --manifest-path src-tauri/Cargo.toml --locked`, `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings`, `npm --prefix ui test -- --run`, `npm --prefix ui run check`, `npm --prefix ui run test:e2e`, `npm --prefix ui run build`, desktop/mobile browser screenshot review, `./init.sh`, JSON validation, source-size guard, and `git diff --check` | passed | Added a typed read-only `get_run_status` command behind `main-read`, reused the sanitized active-run status payload from the CLI status path, rendered marker/container state, resources, image, and network metadata after launch, and kept raw logs, command arguments, environment, mounts, and raw inspect payloads out of frontend state. |
| 2026-06-16 | Tauri launch resource warnings | `cargo test --manifest-path src-tauri/Cargo.toml --locked active_run -- --nocapture`, `cargo test --manifest-path src-tauri/Cargo.toml --locked`, `npm --prefix ui test -- --run`, `npm --prefix ui run check`, `npm --prefix ui run test:e2e`, `npm --prefix ui run build`, desktop/mobile browser screenshot review, `./init.sh`, JSON validation, source-size guard, and `git diff --check` | passed | Added dynamic active-run and material-memory warning codes to the Rust plan and launch-confirmation path. Launch still requires explicit acknowledgement of every warning. The post-launch UI shows a sanitized run snapshot with run id, status, profile, workspace, state volume, network mode, and container name. Raw log feedback remains a separate design because logs can contain sensitive workspace output. |
| 2026-06-16 | Tauri image readiness | `cargo test --locked profile_image_status`, `cargo test --manifest-path src-tauri/Cargo.toml --locked`, `npm --prefix ui test -- --run`, `npm --prefix ui run check`, `npm --prefix ui run test:e2e`, `cargo test --locked`, `cargo clippy --all-targets --locked -- -D warnings`, `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings`, `npm --prefix ui run build`, browser screenshot review, `./init.sh`, JSON validation, source-size guard, and `git diff --check` | passed | Added structured image and builder status from the existing image doctor path, typed Tauri `get_image_status`, `main-read` permission for that command, image-readiness launch enforcement, and launch-panel UI. Missing or stale bundled profile images now block desktop launch before the background run starts; future custom images remain an explicit warning-confirmed advanced path. |
| 2026-06-16 | Tauri launch execution | `cargo fmt`, `cargo test --manifest-path src-tauri/Cargo.toml --locked`, `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings`, `cargo test --locked`, `npm --prefix ui run check`, `npm --prefix ui test -- --run`, `npm --prefix ui run build`, `npm --prefix ui run test:e2e`, source-size guard, `git diff --check`, and `./init.sh` | passed | Added a shared Rust launch runtime and state-volume lock module, typed Tauri `launch_run` command, separate `launch-run` capability, Svelte launch confirmation controls, frontend launch helper, and Rust/frontend/Playwright tests. Launch requires plan confirmation and every warning acknowledgement, blocks when setup checks fail, and does not expose a generic shell/process/container bridge. Run-control operations remain future UI slices. |
| 2026-06-16 | Tauri/Svelte first scaffold | `cargo fmt --check`, `cargo test --locked`, `cargo clippy --all-targets -- -D warnings`, `cargo run --locked --bin runhaven-check-pins`, `npm --prefix ui ci --ignore-scripts`, `npm --prefix ui run check`, `npm --prefix ui test`, `npm --prefix ui run test:e2e`, `npm --prefix ui run build`, `cargo fmt --manifest-path src-tauri/Cargo.toml --check`, `cargo test --manifest-path src-tauri/Cargo.toml --locked`, `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets --locked -- -D warnings`, `npm --prefix ui run tauri:build`, `cargo build --locked`, and `./init.sh` | passed | Added the exact-pinned Svelte/Vite frontend under `ui/` and Tauri v2 shell under `src-tauri/`. The first scaffold exposed read-only setup, dashboard, profile, folder-pick, and run-plan review surfaces only. `main-read` allowed only the four scaffold commands; `folder-pick` allowed only `dialog:allow-open`. The later launch slice is recorded separately. Playwright catches blank-page and console-error regressions. |
| 2026-06-16 | Tauri/UI research completion | Antigravity read-only research, current official Tauri/Tauri plugin/Vite/Svelte/React/Solid/Vue/comparable-product source review, current npm and Cargo package-version checks, JSON validation, local Markdown link check, stale-text scan, `cargo run --locked --bin runhaven-check-pins`, and `git diff --check` | passed | Completed `docs/TAURI_UI_RESEARCH_PLAN.md` with Tauri v2 architecture, Svelte + Vite + TypeScript recommendation, npm lockfile decision, secure-default warning policy, information architecture, command contract, capability map, and first scaffold acceptance criteria. Added the project-wide secure easy path and explicit override rule to `docs/SECURITY_MODEL.md`. No Tauri scaffold or frontend dependencies were added. |
| 2026-06-16 | Tauri/UI research phase setup | Antigravity read-only research, current official Tauri/Vite/frontend source review, comparable UI source review, JSON validation, local Markdown link check, stale-text scan, `cargo run --locked --bin runhaven-check-pins`, and `git diff --check` | passed | Added `docs/NON_UI_BACKLOG.md` as the consolidated non-UI backlog, added `docs/TAURI_UI_RESEARCH_PLAN.md` as the active Tauri/UI research plan, promoted the phase in roadmap/state files, and wired both planning docs into the harness manifest. No Tauri scaffold or frontend dependencies were added. |
| 2026-06-16 | Non-UI CLI and harness cleanup | `cargo fmt --check`, `cargo test --locked`, `cargo clippy --all-targets -- -D warnings`, `cargo run --locked --bin runhaven-check-pins`, `cargo build --locked`, `./init.sh`, expanded `why` CLI smokes, JSON validation, local Markdown link check, Rust source size guard, and `git diff --check` | passed | Added `why workspace`, `why network`, and `why state`; enforced maintained script and image-template descriptions in the pin checker; documented deny-by-default extension/MCP boundaries; refreshed usage, capabilities, roadmap, feature, and current-state docs. |
| 2026-06-16 | Apple Container playbook, script headers, `.gitignore`, and Rust expert review | Apple Container expert review, Antigravity sandbox research, Rust expert review, script/image header scan, shell syntax checks, `cargo fmt --check`, `cargo test --locked`, `cargo clippy --all-targets -- -D warnings`, `cargo run --locked --bin runhaven-check-pins`, `cargo build --locked`, `scripts/apple_container_smoke.sh --with-provider`, `scripts/apple_container_smoke.sh --with-ssh`, cleanup checks, tracked-ignored check, JSON validation, local Markdown link check, Rust source size guard, and `git diff --check` | passed | Added the Apple `container` update playbook, top-of-file descriptions for maintained scripts and image templates, and a refreshed `.gitignore`. Fixed Rust expert findings for auth-broker upstream timeouts, run-record failure safety, cleanup evidence, `publish = false`, and source-size hygiene. The live provider smoke exposed that Apple `container` 1.0.0 gateway addresses are not bindable on macOS; provider proxy and Codex broker now use a wildcard listener only with explicit Apple-container subnet rejection, and focused tests cover off-subnet 403 responses. |
| 2026-06-16 | SSH forwarding fail-closed guard | Planner and CLI regression tests, `scripts/apple_container_smoke.sh`, `scripts/apple_container_smoke.sh --with-ssh`, direct `runhaven plan shell --ssh` smoke, cleanup checks, pin policy, JSON validation, local Markdown link check, active-doc platform/stale-command scan, Rust source size scan, and `git diff --check` | passed | `--ssh` now refuses in plan and run paths before launch while Apple `container` 1.0.0 non-root forwarding is blocked. The guard keeps raw SSH keys unmounted and avoids switching the default agent user to root. |
| 2026-06-16 | SSH forwarding smoke coverage | Apple Container expert review, default `--ssh` plan smoke, `bash -n scripts/apple_container_smoke.sh`, default `scripts/apple_container_smoke.sh`, expected-fail `scripts/apple_container_smoke.sh --with-ssh`, cleanup checks with `target/debug/runhaven runs active`, `target/debug/runhaven state list`, and `target/debug/runhaven network list`, `cargo run --locked --bin runhaven-check-pins`, JSON validation, local Markdown link check, active-doc platform/stale-command scan, and `git diff --check` | blocked | The path-only SSH smoke was rejected as a false positive. The corrected no-secret live smoke must prove disposable-agent connectivity with `ssh-add -l`; on Apple `container` 1.0.0 the non-root `agent` user sees the forwarded socket but gets `Error connecting to agent: Permission denied`. The script now treats that as a live SSH smoke failure. |
| 2026-06-16 | Provider local-network/privacy troubleshooting | Apple Container expert review feedback, `scripts/apple_container_smoke.sh --with-provider`, cleanup checks with `target/debug/runhaven runs active`, `target/debug/runhaven state list`, and `target/debug/runhaven network list`, `cargo run --locked --bin runhaven-check-pins`, JSON validation, local Markdown link check, active-doc platform/stale-command scan, and `git diff --check` | passed | Added user-facing provider troubleshooting that separates policy blocked-host reviews from host-side proxy reachability failures. The live provider smoke passed on macOS 26.5.1 without a Local Network privacy prompt or guest-to-host proxy failure, and cleanup left no active runs, no RunHaven state volumes, and only the shared `runhaven-volume-prep-internal` network. |
| 2026-06-16 | Tauri UI guardrails and backlog update | Official Tauri v2 security, capabilities, and permissions docs review, `cargo run --locked --bin runhaven-check-pins`, JSON validation, local Markdown link check, active-doc platform/stale-command scan, and `git diff --check` | passed | Added `docs/TAURI_UI_GUARDRAILS.md` as the pre-implementation contract for future Tauri resource warnings, approval gates, typed Rust commands, narrow capabilities, and denied-by-default WebView access. Updated roadmap/backlog docs with the accepted task that every maintained script needs a short top-of-file description. |
| 2026-06-16 | Apple Container builder diagnostics | Apple Container CLI/source review, focused image-doctor parser tests, `cargo fmt --check`, `cargo test --locked`, `cargo clippy --all-targets -- -D warnings`, `cargo run --locked --bin runhaven-check-pins`, live `runhaven image doctor shell`, live `runhaven setup --agent shell`, `scripts/apple_container_smoke.sh`, JSON validation, Markdown link check, source size scan, and `git diff --check` | passed | Extended read-only `image doctor` with sanitized `container builder status --format json` diagnostics: builder state, image, CPU/memory, Rosetta mode, start time, and network address. Missing-builder fallback reads configured defaults from `container system property list --format json`; unavailable builder diagnostics print guidance and do not fail image/state checks. The command does not start, stop, delete, or resize the builder and does not print builder mounts or environment. |
| 2026-06-16 | Apple Container runtime pin enforcement | Apple Container expert read-only review, local `container system version --format json` and `container system property list --format json` review, `cargo fmt --check`, `cargo test --locked`, `cargo clippy --all-targets -- -D warnings`, `cargo run --locked --bin runhaven-check-pins`, `cargo run --locked --bin runhaven -- doctor`, `scripts/apple_container_smoke.sh`, JSON validation, Markdown link check, source size scan, and `git diff --check` | passed | Extended `runhaven doctor` to fail closed when the installed Apple `container` runtime commit, builder image, vminit image, or Kata kernel drift from `pins.toml`. Installer SHA-256, signing team ID, and kernel SHA-256 remain release-evidence fields until a separate package or filesystem hash check exists. |
| 2026-06-16 | Apple Container JSON parser fixtures | `cargo fmt --check`, `cargo test --locked`, local Apple `container` read-only payload review, and Apple source/docs field-name review | passed | Added trimmed fixtures for Apple `container` 1.0.0 image list, network inspect, container inspect, legacy inspect attachment aliases, and missing-container stderr classification. The fixtures caught and fixed image-label path handling, restricted image-name parsing to trusted name annotation keys, and tightened repair cleanup so unrelated `not found` errors stay unverified instead of removing active markers. |
| 2026-06-16 | Apple Container smoke harness and provider relay | `bash -n scripts/apple_container_smoke.sh`, `cargo fmt --check`, `cargo test --locked provider::egress -- --nocapture`, `scripts/apple_container_smoke.sh`, `scripts/apple_container_smoke.sh --with-provider`, cleanup checks with `runhaven state list`, `runhaven network list`, and `runhaven runs active` | passed | Added `scripts/apple_container_smoke.sh` as an opt-in local smoke for `runhaven doctor`, shell image readiness, internal read-only `/workspace`, active-run status/logs-follow/stop/repair cleanup, provider plan guidance, optional `--with-provider` allowlist checks, and exact temporary resource cleanup. The live provider smoke exposed and verified a fix for the Rust CONNECT proxy relay: accepted/tunnel sockets are returned to blocking mode and CONNECT header reads no longer consume tunneled bytes. Cleanup left no RunHaven state volumes, no active runs, and only the shared `runhaven-volume-prep-internal` network. |
| 2026-06-16 | Alpha CI disablement and Apple Container gap-analysis setup | `cargo fmt --check`, `cargo test --locked`, `cargo clippy --all-targets -- -D warnings`, `cargo run --locked --bin runhaven-check-pins`, `cargo build --locked`, JSON validation, local Markdown link check, active workflow file scan, stale active-CI scan, `runhaven doctor`, `runhaven plan shell`, `runhaven image build shell --dry-run`, `container system version`, `container system property list`, and `git diff --check` | passed | Removed the active GitHub Actions workflow locally, updated CI pin-policy guidance, captured current Apple Container host evidence, and added an Apple Container pre-Tauri gap analysis plus backlog items for Apple Container and Rust expert review. Apple Container expert read-only review requested changes that are now captured as gap-analysis actions. |
| 2026-06-16 | Capabilities docs cleanup | `cargo run --locked --bin runhaven-check-pins`, local Markdown link check, platform/stale-command scan, `git diff --check`, `cargo run --locked --bin runhaven -- agents`, and `cargo run --locked --bin runhaven -- plan shell --workspace . -- /bin/bash -lc pwd` | passed | Reworked `docs/CAPABILITIES.md` from dense prose into a scan-friendly overview with compact tables for runtime defaults, profiles, network modes, credentials, sessions/state, observability, cleanup, and current limits. |
| 2026-06-16 | README entry-point dedupe | `cargo run --locked --bin runhaven-check-pins`, local Markdown link check, platform wording/stale-command scan, duplicate-section scan, `git diff --check`, and `cargo run --locked --bin runhaven -- plan shell --workspace . -- /bin/bash -lc pwd` | passed | Reduced the main README from a mini-manual to a concise entry point: one alpha/pre-release banner, one product/value section, one quick-start path, and links to detailed capability, usage, security, and architecture docs. |
| 2026-06-16 | Final cleanup before main push | `./init.sh`, direct CLI smokes, JSON validation, local Markdown link check, active-doc stale-reference scan, cleanup scan, Rust source size guard, and `git diff --check` | passed | Refreshed active product and harness docs for the Rust conversion, removed ignored local Python cache/build leftovers and `.DS_Store` files, verified no old Python project guidance remains in active docs, and confirmed every Rust source file remains under 500 lines. |
| 2026-06-16 | Python-to-Rust conversion | `cargo fmt --check`, `cargo test --locked`, `cargo clippy --all-targets -- -D warnings`, `cargo run --locked --bin runhaven-check-pins`, `cargo build --locked`, `./init.sh`, direct CLI smokes, and Rust source size scan | passed | Replaced the Python app, tests, scripts, packaging, and pin checker with a Rust 1.96.0 Cargo crate, exact-pinned dependencies, organized Rust modules under `src/runhaven/`, top-level image templates, and updated harness/docs/CI routing. CLI smokes covered `agents`, `plan shell`, `doctor`, and `image build shell --dry-run`. No Rust source file is over 500 lines. |
| 2026-06-16 | Repo harness contract cleanup | pin check, JSON validation, Markdown link check, platform scan, diff check, advisory HarnessForge report/audit, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Consolidated active state into `current-state.md`, retired `progress.md` and `session-handoff.md`, wired the repo-local harness skill, recorded first-agent and high-risk surface review evidence, and refreshed manifest metadata. HarnessForge audit reported 100/100; report drift actionable was 0. Report readiness remains `warning` because current HarnessForge logic does not consume target-side high-risk surface acceptance evidence. |
| 2026-06-14 | Harness initialization | `harnessforge init --target .` | passed | Existing `AGENTS.md` was preserved; missing harness files were created. |
| 2026-06-14 | Harness audit | `python3 -m harnessforge audit --target . --min-score 85` | passed | Reported 100/100 after the macOS-only correction. |
| 2026-06-14 | POSIX entrypoint | `PYTHON=<temporary-venv-python> ./init.sh` | passed | Ran compileall, unit tests, pin policy, ruff, mypy, and build. |
| 2026-06-14 | macOS-only support | source and docs review | passed | Removed the non-macOS verification entrypoint and unsupported platform claims. |
| 2026-06-14 | Project logo | `magick identify docs/assets/logo.png` plus visual inspection | passed | Tracked logo asset is a stripped 512x512 PNG used by `README.md`. |
| 2026-06-14 | Harness audit | `python3 -m harnessforge audit --target . --min-score 85` | passed | Current score is 100/100 after removing non-macOS verification surfaces. |
| 2026-06-14 | RunHaven rename | source checks, temporary-venv static checks, build, wheel smoke, no-ignore old-name scan, harness audit | passed | Package, module, command, image tags, docs, tests, and harness state use RunHaven/`runhaven`; ignored local virtualenvs were removed because they encoded stale checkout paths. |
| 2026-06-14 | Runtime hardening and macOS-only boundary | unit tests, static checks, build, `./init.sh`, harness audit, `runhaven doctor`, `runhaven state list`, internal-network smoke, and `runhaven plan` smoke | passed | Command validation, unsafe overrides, TTY controls, state commands, host-only internal network creation, and macOS 26+ only verification are covered. |
| 2026-06-14 | Follow-up hardening pass | focused unit tests, full unit suite, and pin check | passed | Added root group rejection, parser help cwd safety, dynamic image template pin discovery, and run/doctor edge-case coverage. |
| 2026-06-14 | Cleanup pass | stale-reference scan, pin check, JSON validation, diff check, and HarnessForge audit | passed | Removed stale local paths, stale local-venv evidence, and old HarnessForge predecessor references from tracked docs. |
| 2026-06-14 | Second follow-up hardening pass | sandboxed Antigravity audit, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, help/plan/doctor smokes, HarnessForge audit, cleanup scans | passed | Added fail-closed network mode validation, leading-zero root identity rejection, sensitive macOS system path blocking, doctor remedies, agent-argument help, and macOS-only pin-ledger enforcement. |
| 2026-06-14 | Provider egress preparation | Playwright-rendered Apple DocC review, complete user-supplied DocC snapshot review, focused fail-closed tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, doctor smoke, and HarnessForge audit | passed | Added reserved `--network provider` mode that failed closed at that stage, explicit plan egress status, and docs stating internet mode remained unrestricted until enforcement was proven. The complete snapshot covered 1,022 rendered Markdown pages plus raw JSON with zero fetch failures and no exact hits for egress or allowlist control terms. |
| 2026-06-14 | Provider egress proxy smoke | allowlist proxy unit tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, default host live smoke, and `api.openai.com` live smoke | passed | Proved the host CONNECT proxy pattern on an internal Apple `container` network: allowed proxied HTTPS succeeded; denied proxied host, proxied IP literal, direct DNS, and direct IP paths failed. At that stage, normal `--network provider` runs remained fail-closed until lifecycle integration landed. HarnessForge audit was skipped for this pass by user instruction while the sibling repo was being worked on. |
| 2026-06-14 | Provider wording cleanup | focused CLI tests, ruff, mypy, and diff check | passed | Aligned CLI help and roadmap wording with the smoke-proven but not yet normal-run-integrated provider proxy state. |
| 2026-06-14 | Provider runtime integration | focused lifecycle tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, and live `runhaven run shell --network provider --provider-host example.com` smoke | passed | Integrated the proxy lifecycle into normal provider runs. The live run allowed proxied HTTPS and denied proxied host, proxied IP literal, direct DNS, and direct IP paths; cleanup left no provider network or test state volume. |
| 2026-06-14 | Provider host guard cleanup | focused planner and CLI tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, ruff, and mypy | passed | Rejected single-label provider hosts such as `com` so explicit provider additions must be fully qualified. Docs now state that a listed host permits that host and its subdomains. |
| 2026-06-14 | Supplemental Apple container source review | direct web source review and Antigravity research pass | passed | Reviewed Apple Open Source, DeepWiki, Wikipedia, The Register, HowToUseLinux, Apidog, and Suraj Deshmukh references. Added durable research notes and reinforced that RunHaven should avoid `container machine` home-mount defaults. |
| 2026-06-14 | Provider blocked-host diagnostics | focused egress, CLI, and plan tests, `PYTHON=<temporary-venv-python> ./init.sh`, Python 3.13 unit suite, and live `runhaven run shell --network provider --provider-host example.com` diagnostic smoke | passed | Provider proxy denials are recorded as capped unique host/port pairs and summarized after the run. The live smoke reported denied `iana.org:443` and cleanup left no provider network or test state volume. |
| 2026-06-15 | Provider diagnostics and DNS guard | focused proxy and CLI tests, manual `why host` and `egress log` checks, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Provider proxy now rejects allowed hosts that resolve to non-public addresses, aggregates and persists provider policy decisions, and exposes first-pass `runhaven why host ...` diagnostics. |
| 2026-06-15 | Provider endpoint matrix | provider source review, pinned package scan, focused CLI/plan tests, full unit suite, ruff, mypy, pin check, diff check, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added structured provider endpoint metadata, source-backed bundled auth and routing hosts, explicit-review endpoint guidance in `runhaven why host`, and `docs/PROVIDER_ENDPOINTS.md`. |
| 2026-06-15 | Grouped blocked-host review and profile smoke | focused CLI and smoke-script tests, full unit suite, ruff, mypy, pin check, diff check, and live `scripts/provider_egress_smoke.py --agent codex` | passed | Provider runs now print grouped blocked-host review with run id, reason, count, rule, and suggested next action. The smoke harness can test bundled profile hosts; the Codex smoke allowed proxied `api.openai.com` and `chatgpt.com` while blocking denied host, proxied IP literal, direct DNS, and direct IP paths. |
| 2026-06-15 | Auth broker boundary diagnostics | focused auth CLI tests, manual `auth status` and `auth explain codex` smokes, provider auth source review, docs review, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added static secret-free auth broker metadata, `runhaven auth status`, `runhaven auth explain AGENT`, JSON output, and `docs/AUTH_BROKER.md`; no credential store or environment values are read. |
| 2026-06-15 | Empty-allowlist regression coverage | focused planner and egress tests plus `PYTHON=<temporary-venv-python> ./init.sh` | passed | Internet mode is explicitly unrestricted with no provider allowlist, internal mode is local-only with no provider allowlist, provider mode fails closed with an empty allowlist, and `EgressPolicy` rejects empty allowlists. |
| 2026-06-15 | Codex API-key broker prototype | focused planner/proxy/CLI tests, manual broker plan/dry-run/missing-env/auth smokes, official Codex config source review, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added opt-in `--codex-api-key-broker-env NAME` for Codex provider runs. The host broker keeps the raw API key out of the planned command and guest environment, restricts clients to the provider subnet, and forwards only Responses API create requests. |
| 2026-06-15 | Codex broker observability and smoke harness | focused broker/log/smoke tests, full unit suite, ruff, mypy, pin check, diff check, manual `auth log`, no-key smoke skip, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added sanitized broker decisions, `runhaven auth log`, secret-free auth broker JSONL entries, and optional `scripts/codex_broker_smoke.py`. The live provider request was skipped because no disposable API key was set. |
| 2026-06-15 | Run observability | red/green focused CLI tests, full CLI suite, manual `runs list/show` smoke, ruff, mypy, pin check, JSON validation, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runs.jsonl`, `runhaven runs list`, and `runhaven runs show RUN_ID`. Run records summarize provider policy, auth broker, and cleanup outcomes without command lines, agent arguments, env names, env values, request bodies, or token values. |
| 2026-06-15 | Joined run logs | red/green focused CLI tests, full CLI suite, manual `runs log` text/JSON smoke, ruff, mypy, pin check, JSON validation, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs log RUN_ID` to join the run record with matching provider policy and auth broker entries for the same run id. Text and JSON output remain secret-free. |
| 2026-06-15 | Git run metadata | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, manual `runs show/log` git smoke, cleanup scan, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Run records now include git repo root, before and after `HEAD`, dirty state, changed count, and capped relative paths for git workspaces. Records still omit diffs, file contents, prompts, commands, env details, request bodies, and token values. |
| 2026-06-15 | Live run diffs | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, manual `runs diff` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs diff RUN_ID`. It prints live git diff output for committed, tracked dirty, untracked dirty, and mixed committed-and-dirty changes only after recorded repo root, `HEAD`, dirty state, changed count, and path set still match the workspace, and refuses stale or unavailable metadata. |
| 2026-06-15 | Active run stop | red/green focused CLI and plan tests, full unit suite, ruff, mypy, pin check, manual `runs stop` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added named RunHaven containers, secret-free active-run markers, `runhaven runs stop RUN_ID`, and stopped status for runs that exit after a stop request. |
| 2026-06-15 | Active run discovery | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs active` text/JSON smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs active` to list current active-run markers without command lines, agent arguments, env details, prompts, request bodies, token values, or Apple `container` CLI access. |
| 2026-06-15 | Active run attach | red/green focused CLI tests, local Apple `container exec --help`/`container attach --help` review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs attach` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs attach RUN_ID` to open a guarded `container exec` shell or command in an active RunHaven-owned container. |
| 2026-06-15 | Active run log follow | red/green focused CLI tests, local Apple `container logs --help` review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs logs-follow` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs logs-follow RUN_ID` to follow recent and live logs for an active RunHaven-owned container. |
| 2026-06-15 | Active run status | red/green focused CLI tests, local Apple `container inspect --help`/JSON review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs status` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs status RUN_ID` to show sanitized active marker plus live container state without raw inspect arguments, environment, or mount fields. |
| 2026-06-15 | Active run kill | red/green focused CLI tests, local Apple `container kill --help` review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs kill` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs kill RUN_ID` as the explicit hard-stop recovery path for an active RunHaven-owned container and record killed runs as `killed`. |
| 2026-06-15 | Active run repair | red/green focused CLI tests, local Apple missing-container inspect review, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs repair` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs repair RUN_ID` to remove a stale active marker only after Apple `container inspect` reports the recorded RunHaven-owned container is not found. |
| 2026-06-15 | Bulk active run repair | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runs repair --all` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runhaven runs repair --all` to inspect valid active markers and remove only those whose recorded RunHaven-owned container is confirmed missing. |
| 2026-06-15 | Repair JSON output | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual repair JSON smokes, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `--json` to `runhaven runs repair RUN_ID` and `runhaven runs repair --all` with secret-free results, counts, and exit codes. |
| 2026-06-15 | Guided setup | red/green focused CLI tests, full unit suite, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runhaven setup --agent shell` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added non-mutating `runhaven setup` to run prerequisite checks, print exact fixes, and show profile-specific first-run commands. |
| 2026-06-15 | Setup network guidance | red/green focused CLI test, focused setup/doctor tests, full unit suite, compileall, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runhaven setup --agent shell` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added goal-based network guidance to `runhaven setup` for local-only, provider-only, package install, and unrestricted internet runs. |
| 2026-06-15 | Setup workspace and credential guidance | red/green focused CLI test, focused setup/doctor tests, full unit suite, compileall, ruff, mypy, pin check, JSON validation, Markdown link check, platform scan, manual `runhaven setup --agent shell` smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added setup guidance for smallest-project workspace scope, `/workspace` mount semantics, avoided host credential paths, `--ssh`, and reviewed `--env NAME` usage. |
| 2026-06-15 | First modularization extraction | structural review, focused setup and active-run tests, full unit suite, compileall, ruff, mypy, pin check, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added a modularization plan and extracted setup guide output, active-run marker persistence, cache path helpers, and shared validators from `src/runhaven/cli.py`. |
| 2026-06-15 | Run-history modularization extraction | structural review, focused run-history tests, full unit suite, compileall, ruff, mypy, pin check, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/run_history.py` for run-record persistence, git metadata capture, run-record readers, and `runs list/show/log/diff`; `src/runhaven/cli.py` is now 1,874 lines. |
| 2026-06-15 | Active-command modularization extraction | structural review, focused active-command tests, full unit suite, compileall, ruff, mypy, pin check, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/active_commands.py` for `runs active/status/attach/logs-follow/stop/kill/repair`; `src/runhaven/cli.py` is now 1,411 lines. |
| 2026-06-15 | Provider-runtime modularization extraction | blog source review, structural review, focused provider-runtime tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/provider_runtime.py` for provider run lifecycle, proxy/broker startup, decision logging, cleanup, and internal-network inspection; `src/runhaven/cli.py` is now 1,005 lines. |
| 2026-06-15 | Diagnostic-command modularization extraction | structural review, focused diagnostic tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/diagnostic_commands.py` for `auth status/explain/log`, `egress log`, `why host`, and diagnostic log readers; `src/runhaven/cli.py` is now 767 lines. |
| 2026-06-15 | CLI test modularization split | structural review, focused split CLI tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Split the 90 CLI tests from `tests/test_cli.py` into focused test files plus `tests/cli_test_helpers.py`; `tests/test_cli.py` is now 228 lines. |
| 2026-06-15 | Active-command CLI test split | structural review, focused active-command CLI tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Split the 33 active-command tests from `tests/test_cli_active_commands.py` into focused list, attach/logs, status, and stop/kill test files. |
| 2026-06-15 | Run-history CLI test split | structural review, focused run-history CLI tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Split the 12 run-history CLI tests from `tests/test_cli_run_history.py` into focused list/show, diff, and log test files. |
| 2026-06-15 | Provider-runtime CLI test split | structural review, focused provider-runtime CLI tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Split the 12 provider-runtime CLI tests from `tests/test_cli_provider_runtime.py` into focused proxy, Codex broker, and internal-network test files. |
| 2026-06-15 | Git-metadata extraction | structural review, focused CLI tests, compileall, ruff, mypy, full unit suite, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/git_metadata.py` for git discovery, status parsing, run git summaries, and live diff helpers; `src/runhaven/run_history.py` is now 383 lines. |
| 2026-06-15 | Active-repair extraction | structural review, focused active-command CLI tests, compileall, ruff, mypy, full unit suite, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/active_repair.py` for stale-marker repair, JSON payloads, exit-code rules, and inspect-missing validation; `src/runhaven/active_commands.py` is now 342 lines. |
| 2026-06-15 | NPM pin-policy extraction | structural review, focused pin-policy checks, compileall, ruff, mypy, full unit suite, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `scripts/npm_pin_policy.py` for package.json and package-lock policy; `scripts/check_pins.py` is now 380 lines and remains the entrypoint. |
| 2026-06-15 | Auth-profile extraction | structural review, focused auth broker and diagnostic checks, compileall, ruff, mypy, full unit suite, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/auth_profiles.py` for static auth broker profile metadata; `src/runhaven/auth_broker.py` is now 374 lines. |
| 2026-06-15 | Provider-observability extraction | structural review, focused provider, diagnostic, and log checks, compileall, ruff, mypy, full unit suite, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/provider_observability.py` for provider and auth log writes plus blocked-host review text; `src/runhaven/provider_runtime.py` is now 379 lines. |
| 2026-06-15 | CLI parser extraction | structural review, focused CLI parser checks, compileall, ruff, mypy, full unit suite, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `src/runhaven/cli_parser.py` for argparse construction; `src/runhaven/cli.py` is now 472 lines. |
| 2026-06-15 | Active-repair test cleanup | structural review, focused active-repair tests, compileall, ruff, full unit suite, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Reused `write_active_marker` in `tests/test_cli_active_repair.py`; file is now 401 lines and remains cohesive. |
| 2026-06-15 | Workspace scope selection | focused planner, CLI, and standard-run tests, compileall, ruff, mypy, full unit suite, pin check, docs checks, manual plan smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `--workspace-scope` with `current` and `git-root`; default current scope does not silently broaden git subdirectory mounts, and git-root expansion is explicit. |
| 2026-06-15 | Worktree run isolation | focused standard-run tests, compileall, ruff, mypy, full unit suite, pin check, docs checks, manual worktree dry-run smoke, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `run --worktree` with clean-source enforcement, RunHaven-owned git branch/worktree creation, isolated mount, and run-record recovery commands. |
| 2026-06-15 | README documentation split | `git diff --check`, `python3 scripts/check_pins.py`, `python3 -m json.tool feature_list.json`, local Markdown link check, and platform wording scan | passed | Shortened `README.md` into a project entry point; added `docs/INSTALLATION.md` and `docs/CAPABILITIES.md`; kept command workflows in `docs/USAGE.md`. |
| 2026-06-15 | Worktree lifecycle commands | red/green worktree lifecycle tests, adjacent run-history/worktree tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `runs keep`, `runs merge`, and `runs discard` with RunHaven-owned worktree validation, source HEAD and dirty checks for merge, committed and dirty change application, and explicit discard cleanup. |
| 2026-06-15 | Worktree merge recovery guidance | red/green focused lifecycle test, adjacent worktree/run-history tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Failed pre-cleanup `runs merge` attempts now keep the recorded worktree and branch and print source, worktree, branch, review, retry, keep, and discard commands. |
| 2026-06-15 | Worktree manual recovery command | red/green focused lifecycle test, adjacent worktree/run-history tests, full unit suite, compileall, ruff, mypy, pin check, docs checks, `runhaven runs --help`, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added read-only `runs recover` with source/worktree status and numbered manual recovery steps. |
| 2026-06-15 | Worktree recovery JSON and dirty-source guidance | red/green focused CLI tests, adjacent worktree/run-history tests, full unit suite, compileall, ruff, mypy, pin check, JSON validation, Markdown link check, `runhaven runs recover --help`, `git diff --check`, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added read-only `runs recover --json` for automation and clearer dirty-source choices before `run --worktree` creates any worktree. |
| 2026-06-15 | Worktree project check suggestions | red/green focused lifecycle tests, adjacent worktree/run-history tests, full unit suite, compileall, ruff, mypy, pin check, JSON validation, Markdown link check, `runhaven runs recover --help`, `git diff --check`, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added advisory detected project check suggestions to `runs keep`, `runs recover`, and `runs recover --json`; suggestions are not run automatically. |
| 2026-06-15 | Warm reusable project sessions | red/green focused planner, core CLI, state, and standard-run tests; full unit suite; compileall; ruff; mypy; pin check; JSON validation; Markdown link check; `runhaven run --help`; `runhaven state reset --help`; `runhaven plan shell --session review`; `git diff --check`; and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Added `--session NAME`, session-aware run records and active markers, `state reset`, `state list --session`, and session-filtered `state prune` without changing default volume names. |
| 2026-06-15 | Image and managed-network repair UX | red/green focused CLI tests, full harness verification, `image rebuild --dry-run`, `network list`, `network prune`, JSON validation, Markdown link check, and `git diff --check` | passed | Added `runhaven image rebuild AGENT`, `runhaven network list`, and guarded `runhaven network prune --yes`; network deletion is limited to RunHaven-managed network names. |
| 2026-06-15 | Image doctor and preflight recovery diagnostics | red/green focused image-doctor tests, adjacent CLI tests, compileall, ruff, mypy, full harness verification, `image doctor --help`, `image doctor shell`, JSON validation, Markdown link check, and `git diff --check` | passed | Added read-only `runhaven image doctor [AGENT]` to detect missing bundled image tags and print rebuild, network, and state recovery guidance without mutating resources. |
| 2026-06-15 | Image doctor source metadata and state review | red/green focused image and state-volume tests, adjacent image/state/active tests, compileall, ruff, mypy, full harness verification, `image doctor shell`, and `image build shell --dry-run` | passed | Added RunHaven source-digest labels to bundled image builds, stale-image detection in `image doctor`, and read-only inactive state-volume review. |
| 2026-06-15 | Repository documentation accuracy refresh | stale-reference scans, JSON validation, pin check, Markdown link check, platform wording scan, HarnessForge audit, `git diff --check`, and `PYTHON=<temporary-venv-python> ./init.sh` | passed | Refreshed the current objective, component inventory, modularization snapshot, roadmap status note, and home-volume wording. HarnessForge reported 100/100; full harness ran 195 unit tests plus compileall, pin check, ruff, mypy, and build. |
| 2026-06-15 | First-agent harness task retirement | JSON validation, pin check, Markdown link check, platform wording scan, HarnessForge drift report, HarnessForge report, HarnessForge audit, and `git diff --check` | passed | Marked the first-agent review task retired, updated startup routing and operating-loop wording, moved the repo harness overhaul to validated, and refreshed manifest metadata so drift is clean. HarnessForge report warnings are advisory while the tool is under active development. |
| 2026-06-15 | Manual harness documentation refresh | JSON validation for `feature_list.json` and all harness JSON files, local Markdown link check, platform wording scan, `python3 scripts/check_pins.py`, `git diff --check`, HarnessForge dry-run drift report, HarnessForge report, and HarnessForge audit | passed | Manually inspected all 29 files under `docs/harness/`; corrected stale lifecycle, advisory-tooling, privacy-label, source-record, modularization, and backlog-status wording. HarnessForge had no actionable generated drift and audit `100/100`; new report suggestions remain advisory. Full `./init.sh` was skipped because no runtime code changed. |

Rules:

- Record command name, scope, result, and risk.
- Do not paste secrets, local absolute paths, or long command output.
- Prefer one current row per meaningful verification event.
